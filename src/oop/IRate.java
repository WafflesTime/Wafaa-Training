package oop;

public interface IRate {
	
	// writes class outline. (tells the class what methods they need to have and implement)
	// Interfaces are properties. Hence BankAccount has a rate
	//CDAccount is BankAccount's child.
	public void setRate();
	public void increateRate();

}
